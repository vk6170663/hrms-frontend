const Logo = () => {
    return (
        <div className="logo">
            <img src="../../public/logo.svg" />
            <span className="logo-text">Logo</span>
        </div>
    );
};

export default Logo;